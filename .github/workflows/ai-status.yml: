    steps:
      - name: "SYNTHESIZE_HISTORY"
        uses: irongamer727/ai-report-action@v1
        with:
          gh_token: ${{ secrets.GITHUB_TOKEN }}
          render_api_key: ${{ secrets.RENDER_API_KEY }}
          output_to_job_summary: true  # <--- This forces it onto that screen
